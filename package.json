{
  "name": "headless-dns-widget-vue-starter",
  "version": "2.0.0",
  "description": "A Vue starter for the Approximated headless DNS widget API.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node --env-file=.env server.mjs",
    "build": "node build.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "vue": "3.5.42"
  },
  "devDependencies": {
    "vite": "8.3.0",
    "@vitejs/plugin-vue": "6.0.8"
  }
}
