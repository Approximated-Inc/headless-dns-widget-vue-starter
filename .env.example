# SERVER ONLY. Never rename this to VITE_APX_API_KEY or put it in frontend code.
APX_API_KEY=
# Public CNAME target: replace this with the hostname used by your application.
VITE_CNAME_TARGET=domains.example.com
